# Enable mock menu data for development (no backend required)
# Set to 'true' to use mock data instead of API
# Useful for development when backend is not running
VITE_USE_MOCK_MENU=true

# API Base URL
# Change this to match your backend URL
VITE_API_URL=http://localhost:3000
